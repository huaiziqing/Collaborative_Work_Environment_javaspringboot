name: Clojure CI

on:
  push:
    branches: [ "dev_01" ]
  pull_request:
    branches: [ "dev_01" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install dependencies
      run: lein deps
    - name: Run tests
      run: lein test
